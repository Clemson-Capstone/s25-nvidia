{{- if .Values.ragServer.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "nvidia-rag.ragServerName" . }}
  labels:
    {{- include "nvidia-rag.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "nvidia-rag.ragServerName" . }}
  minReplicas: {{ .Values.ragServer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.ragServer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.ragServer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.ragServer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.ragServer.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.ragServer.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}